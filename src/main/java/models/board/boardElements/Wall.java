package models.board.boardElements;

import models.board.character.Turtle;

public class Wall implements BoardElement {

    public void hitTurtle(Turtle turtle) {
        /* Hit a wall does not change anything to the turtle*/
    }
}
